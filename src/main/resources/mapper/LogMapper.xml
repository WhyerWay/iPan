<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="indi.ipan.dao.LogDao">
    <insert id="addLog" parameterType="userOperationLog" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into user_operation_log (username, operation, count) 
        values(#{username}, #{operation}, #{count})
    </insert>
    <update id="addResult" parameterType="userOperationLog">
        update user_operation_log set status = #{status} 
        where id = #{id}
    </update>
</mapper>